# This configuration file was generated by `ameba --gen-config`
# on 2021-09-06 15:06:25 UTC using Ameba version 0.13.4.
# The point is for the user to remove these configuration records
# one by one as the reported problems are removed from the code base.

# Run `ameba --only Lint/UselessAssign` for details
Lint/UselessAssign:
  Description: Disallows useless variable assignments
  Excluded:
    - spec/lib_pigpio_spec.cr
  Enabled: true
  Severity: Warning

# Run `ameba --only Style/VariableNames` for details
Style/VariableNames:
  Description: Enforces variable names to be in underscored case
  Excluded:
    - src/pigpio/lib_pigpio.cr
  Enabled: true
  Severity: Convention
